import { ajax } from 'super-rentals/helpers/ajax';
import { module, test } from 'qunit';

module('Unit | Helper | ajax');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ajax([42]);
  assert.ok(result);
});
